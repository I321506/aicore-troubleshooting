Shared reference: region grouping used by the availability configs.
Change a region's group by moving its short name between the lists in each
config's `region_groups:` block — no code change needed.
